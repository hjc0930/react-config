const dir = require("./dir");
const utils = require("./utils");
const loaders = require("./loaders");
const plugins = require("./plugins");
const mfsu = require("./mfsu");

module.exports = {
  dir,
  utils,
  loaders,
  plugins,
  mfsu
};
